# Homepage files

The protection-first + SEO homepage lives in the Grok workspace as:

- `index.html` (use this for Live Server)
- `index (2) (2).html` (same content)

If GitHub `index.html` is still a short redirect, replace it locally with the workspace file after pull.
